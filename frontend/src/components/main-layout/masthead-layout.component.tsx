import { Typography } from '@mui/material';

export const Masthead: React.FC = () => {
  return (
    <div className='mast'>
      <Typography>a Martin Hema production</Typography>
    </div>
  );
};
